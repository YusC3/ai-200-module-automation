[project]
name = "data_operations"
version="0.1.0"
description = "Performs various data operations on Azure Managed Redis"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
    "redis>=7.0.1",
]
